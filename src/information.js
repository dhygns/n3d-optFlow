

// const webcamWidth = 320;
// const webcamHeight = 240;
const webcamWidth = 320;
const webcamHeight = 240;

const textureWidth = 256;
const textureHeight = 256;

export default {
  webcamWidth,
  webcamHeight,

  textureWidth,
  textureHeight,
}
